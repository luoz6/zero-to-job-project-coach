# Zero To Job Project Coach v2 设计规格

## 目标

将 `zero-to-job-project-coach` 从“项目推荐与简历包装 skill”升级为“主控引擎 + 外挂知识矩阵”的求职推进型 skill。v2 要帮助新手从当前水平出发，经过阶段诊断、岗位拆解、GitHub 项目选型、二次开发验收、简历转化、面试拷问和投递复盘，形成可执行的求职闭环。

## 非目标

- 不承诺 offer、高薪、保过面试或保证录用。
- 不实现 GitHub API 客户端、爬虫、自动投递器或简历导出器。
- 不把五个 reference 写成大型百科；第一版每个 reference 保持可读、可维护，优先覆盖高频岗位和高价值规则。
- 不提交或使用 `docs/superpowers/` 下的规划文件。

## 当前问题

当前 `SKILL.md` 已覆盖搜索、评估、二次开发、简历和面试，但存在三个结构性问题：

- 主文件承载过多知识细节，后续扩展会变长并稀释关键规则。
- “从 0 到找工作”的阶段推进感不足，用户容易跳过项目选择、实操验收和面试防守。
- 岗位路径、项目评分、二开蓝图、简历案例和面试题库没有独立知识库，难以维护和测试。

## 目标文件结构

第一版 v2 创建或修改以下文件：

```text
zero-to-job-project-coach/
  SKILL.md
  agents/
    openai.yaml
  references/
    role_templates.md
    github_scoring_matrix.md
    secondary_dev_blueprints.md
    resume_benchmarks.md
    job_execution_playbook.md
tests/
  test_zero_to_job_project_coach.py
specs/
  zero-to-job-project-coach-v2.md
```

`SKILL.md` 作为主控引擎，只保留调度逻辑、阶段状态机、关键红线和 reference 读取规则。五个 reference 文件承载细节知识。

`agents/openai.yaml` 也必须随 v2 更新，因为 skill 从“项目推荐与包装”升级为“三阶段求职推进引擎”。`default_prompt` 必须显式提到 `$zero-to-job-project-coach`、阶段诊断、项目选择、二次开发、简历和面试准备；`short_description` 应体现“从新手到求职就绪”的阶段推进定位。

## 五大 reference 知识库

### `references/role_templates.md`

定位：岗位路径与里程碑验收库。

必须包含：

- 至少覆盖 Java 后端、前端、AI 应用开发、数据分析、DevOps、全栈、移动端开发七类岗位，不得低于 v1 的岗位覆盖范围。
- 每个岗位包含：底层基线、进阶能力、推荐项目类型、最低简历项目标准、里程碑验收问题。
- Java 后端必须覆盖 Spring Boot、MyBatis、AOP、Redis、消息队列、服务治理或部署工程化。
- AI 应用开发底层基线必须覆盖 RAG、LangChain 或同类编排框架、向量数据库和最小可运行 AI 应用。TextSplitter 对检索效果的影响作为进阶验收点，不作为第一层入门必达要求。

### `references/github_scoring_matrix.md`

定位：GitHub 项目硬核打分矩阵。

必须包含满分 100 分规则：

- 工程规范度 25 分。
- 技术栈含金量 25 分。
- 学习价值与阶段匹配 20 分。
- 二次开发空间 20 分。
- 可见维护信号 10 分。

`学习价值与阶段匹配` 必须评估项目是否适合用户当前水平、是否能拆成可执行学习路径、是否能在用户时间预算内完成一个可展示版本、是否能支持面试讲解。工程规范和技术栈豪华但明显超出用户阶段的项目不得获得高总分。

必须包含一票否决规则：

- 长期未维护且无法运行。
- 缺乏核心源码。
- 完全没有 README 启动说明。
- 只有学习笔记、面试题、awesome list 或项目合集。
- 明显超出用户当前阶段且无法拆成可执行学习路径。

必须要求不要编造精确 issue/PR 统计；只能使用可见维护信号和不确定性描述。

### `references/secondary_dev_blueprints.md`

定位：高阶二次开发蓝图库。

必须包含三级改造：

- Level 1 业务闭环：状态机、支付回调、权限审批、导入导出、通知。
- Level 2 架构与高并发：Caffeine + Redis 多级缓存、消息队列解耦、Canal binlog 同步、异步任务、限流熔断。
- Level 3 治理与性能：动态线程池、压测基准、Prometheus/Grafana、OpenTelemetry、灰度或降级方案。

每个蓝图必须包含：适用场景、技术锚点、改造步骤、可交付物、验收问题、简历表达模板。

### `references/resume_benchmarks.md`

定位：简历标杆与 few-shot 案例库。

必须包含：

- STAR 法则说明。
- 低效表达与高阶表达对比。
- 至少覆盖 Java 后端、前端、AI 应用开发三类项目 bullet。
- 量化指标规则：没有实测数据时必须写“预期目标”或“完成后待实测”，不得写成已完成结果。
- 简历防守清单：每条 bullet 必须能回答场景、技术选型、指标来源、踩坑和回滚方案。

### `references/job_execution_playbook.md`

定位：求职闭环与面试高压题库。

必须包含：

- 面试追问题库，重点覆盖技术选型与踩坑。
- 投递执行表字段：日期、岗位、公司分层、简历版本、投递渠道、反馈、被问问题、盲区、下一步修正。
- 1 到 2 周投递节奏模板。
- 面试复盘机制：每次面试后沉淀高频问题、简历风险点和补强任务。

## SKILL.md 主控引擎

`SKILL.md` 必须瘦身为调度器，包含以下核心章节：

- `## 关键规则`
- `## 输入与冲突处理`
- `## Reference 调度规则`
- `## 状态管理与阶段声明`
- `## 三阶段解锁工作流`
- `## Phase 1：诊断、拆解与选型定调`
- `## Phase 2：硬核开模与验收标准`
- `## Phase 3：交付转化与求职冲刺`
- `## 私有/企业项目处理`
- `## 降级路径`
- `## 输出控制`

### 关键规则

`## 关键规则` 必须放在 `SKILL.md` 前部，并保留 v1 的硬约束：

- 不承诺 offer、高薪、保过面试或保证录用，只承诺提升求职就绪度。
- 不得只因为 star 多就推荐项目，必须结合岗位匹配、学习价值、阶段匹配、简历价值和二次开发空间。
- 推荐 GitHub 仓库前必须尽量联网核实；无法联网时走降级路径并标注“未实时核实”。
- 不得编造 issue 解决率、PR 合并率、维护者响应时间、贡献者趋势或社区活跃度。
- 不得把建议改造、预期目标、待实测指标写成已完成成果。
- Phase 1 和 Phase 2 默认卡点推进；用户要求跳过或预览时必须标注风险。
- 高优先级项目进入二次开发前必须尽量做源码结构评估；无法评估时降低推荐置信度。
- 必须按需读取 reference 文件，不把所有知识库一次性倾倒给用户。
- 每次回复开头必须输出一行阶段状态声明，防止多轮对话中状态漂移。
- 当前阶段不得输出其他阶段的重型内容，例如 Phase 1 不输出正式面试题库，Phase 2 不提前生成正式简历 bullet。

### 输入与冲突处理

v2 不得丢失 v1 的输入处理能力。`SKILL.md` 必须保留：

- 低风险默认假设：用户给 JD 但没说水平时，根据岗位标题、年限和技能推断目标水平；用户只给仓库链接时，直接进入单仓库评估；用户给“Java 后端校招”等常见方向时使用岗位模板默认技术栈。
- 高风险追问：没有目标岗位/JD/仓库链接、岗位方向冲突、要求简历成果但没有完成边界时，提出 1 个简短问题。
- 水平/JD 冲突：优先相信用户自述水平，把 JD 作为差距目标，输出差距分析，不直接按 JD 高阶要求推荐过难项目。

### Reference 调度规则

这是读取 reference 的权威定义。Phase 描述只引用本节，不重复定义条件，避免两处规则不一致。

必须明确什么时候读取哪个 reference：

- 未明确岗位或需要岗位能力拆解时，读取 `references/role_templates.md`。
- 需要搜索、筛选或比较 GitHub 项目时，读取 `references/github_scoring_matrix.md`。
- 用户已选择项目或需要二次开发方案时，读取 `references/secondary_dev_blueprints.md`。
- 用户需要简历 bullet 或项目包装时，读取 `references/resume_benchmarks.md`。
- 用户进入面试准备、投递计划或复盘时，读取 `references/job_execution_playbook.md`。

### Reference 输出边界

“按需读取”首先是行为约束，不依赖底层框架是否真正支持动态文件读取。即使底层上下文已经包含多个 reference，也必须限制当前阶段的输出范围：

- Phase 1 可以使用 `role_templates.md` 和 `github_scoring_matrix.md`，不得输出 `job_execution_playbook.md` 中的高压面试题。
- Phase 2 可以使用 `secondary_dev_blueprints.md`，不得生成正式简历 bullet；最多输出“未来可写成简历的候选表达”。
- Phase 3 可以使用 `resume_benchmarks.md` 和 `job_execution_playbook.md`，但不得把 Phase 2 未完成内容写成已完成成果。
- 窄输出场景只读取完成该目标所需的最小 reference；如果必须跨阶段读取，必须说明原因。

### 状态管理与阶段声明

`SKILL.md` 必须要求每次回复第一行输出可见状态标签，格式固定为：

```text
[当前阶段：Phase X - 简短状态]
```

示例：

- `[当前阶段：Phase 1 - 阶段诊断与项目选型]`
- `[当前阶段：Phase 2 - 二次开发验收中]`
- `[当前阶段：Phase 3 - 简历转化与面试冲刺]`
- `[当前阶段：窄输出 - 简历草稿待验证]`

状态声明必须反映当前实际任务，不得在用户尚未确认项目时声称进入 Phase 2，不得在用户未完成或未通过验收时声称进入 Phase 3。用户指出阶段错误时，必须以用户纠正为准并重新声明阶段。

### Phase 1：诊断、拆解与选型定调

目标：定位用户阶段，确定岗位路径，选出 2 到 3 个高优先级项目。

必须包含：

- 阶段探针诊断：如果用户没说明现状，提出 2 到 3 个核心问题。
- 按 `Reference 调度规则` 读取 `role_templates.md`，对齐岗位技术底线和里程碑。
- 联网搜索 GitHub 项目；无法联网时让用户提供候选仓库链接或 README。
- 按 `Reference 调度规则` 读取 `github_scoring_matrix.md`，给候选仓库打分。
- 输出项目分数、优势、风险、适合人群和推荐优先级。
- 阶段卡点：默认在 Phase 1 结束后停顿，等待用户确认选择项目后再进入 Phase 2。

### Phase 2：硬核开模与验收标准

目标：让用户把选定项目做成可讲、可写、可验收的作品。

必须包含：

- 防卡壳启动路线：最小启动路径、环境检查、常见卡点、降级路径、验收信号、时间盒。
- 源码结构评估：在进入深度二次开发前，核验入口文件、依赖配置、模块边界、测试/CI/部署模板和明显风险；如果无法核验，降低推荐置信度并提示风险。
- 按 `Reference 调度规则` 读取 `secondary_dev_blueprints.md`，为选定项目定制 2 到 3 个二次开发任务。
- 每个任务包含技术锚点、改造步骤、预期交付物、预期指标和验收问题。
- 阶段卡点：要求用户完成改造或至少回答验收问题后，再进入 Phase 3。
- 允许用户显式要求“先预览 Phase 3”，但必须标注未完成实操带来的简历风险。

### Phase 3：交付转化与求职冲刺

目标：把完成的项目成果转化为简历、面试表达和投递执行。

必须包含：

- 按 `Reference 调度规则` 读取 `resume_benchmarks.md`，生成符合 STAR 和量化规则的简历 bullet。
- 区分已完成成果、预期目标和待实测指标。
- 按 `Reference 调度规则` 读取 `job_execution_playbook.md`，生成 3 到 5 轮高压追问。
- 输出 1 到 2 周投递行动计划和复盘表。
- 投递执行表必须使用 Markdown 表格或 CSV 代码块输出，方便复制到 Notion、Excel 或飞书表格。

### 私有/企业项目处理

当用户提供的是非开源项目、私有仓库、实习经历、公司内部项目或纯业务描述时，不强行套用 GitHub 搜索和 GitHub 项目评分。

处理规则：

- 跳过 `github_scoring_matrix.md` 中依赖 GitHub 可见信息的评分项，例如 star、fork、issue、PR、公开维护信号。
- 要求用户提供最小项目材料：业务背景、目标用户、系统模块、技术栈、个人职责、核心难点、可验证结果、是否能公开描述。
- 可以直接进入 Phase 2：基于业务架构摘要和核心难点设计二次开发、补强或复盘任务。
- 可以直接进入 Phase 3：基于用户已完成内容生成简历草稿，但必须标注哪些信息来自用户自述、哪些指标待验证。
- 不得要求用户泄露公司敏感信息、密钥、内部接口、真实客户数据或不可公开代码。

### 阶段卡点与推进条件

默认情况下，Phase 1 和 Phase 2 都需要卡点确认，但用户可以显式改变推进方式：

- 用户确认项目：从 Phase 1 进入 Phase 2。
- 用户完成改造或回答验收问题：从 Phase 2 进入 Phase 3。
- 用户说“跳过卡点”“直接进入下一阶段”“先给完整路线”：可以继续，但必须标注风险。
- 用户说“先预览 Phase 3”：只输出草稿级简历和面试风险，不把未完成改造写成已完成成果。
- 用户长时间不确认时，不应反复催促；应提供可选下一步，例如“选 A 项目继续”“换项目”“只看简历草稿”“只做面试追问”。

### 验收失败纠偏机制

Phase 2 的验收不是形式检查。用户回答验收问题错误、逻辑不完整或暴露关键技术误解时，不能放行进入 Phase 3。

必须执行：

- 指出不合格点：明确哪一处概念、链路、代码逻辑或指标解释有问题。
- 给出提示而不是直接代答：优先提供 Hint、排查方向、参考模块或关键概念。
- 要求用户重答或补充证据：例如架构图、关键代码片段、压测结果、日志、README 改动。
- 可降级：如果用户暂时无法过关，可以输出“草稿级简历/面试风险”，但必须标注“未通过验收，不建议直接写进正式简历”。
- 只有当用户回答达到 `role_templates.md` 的里程碑标准，或用户明确要求跳过并接受风险，才能进入 Phase 3。

### 降级路径

`## 降级路径` 是全局降级汇总，不替代各 Phase 的局部降级。它必须覆盖：

- 无法联网：要求用户提供仓库链接、README 或源码摘要，所有仓库活跃度标注为未实时核实。
- 用户信息不足：提出一个最小必要问题；如果用户不回答，使用低风险默认假设并显式标注假设。
- 无法核验源码骨架：降低推荐置信度，不进入高强度二次开发承诺。
- 用户跳过实操：允许输出草稿，但简历和面试内容必须标注“待验证”。
- reference 未加载或缺失：说明缺失知识库，退回主控规则的最小可用流程。

## 输出契约

默认按阶段输出，不一次性输出所有阶段。

Phase 1 默认输出：

- `当前阶段诊断`
- `目标岗位拆解`
- `GitHub 项目候选与评分`
- `推荐项目排序`
- `Phase 1 卡点：请选择一个项目`

Phase 2 默认输出：

- `防卡壳启动路线`
- `源码结构评估结论`
- `二次开发蓝图`
- `技术锚点与交付物`
- `验收问题`
- `Phase 2 卡点：完成改造或回答验收问题`

Phase 3 默认输出：

- `简历项目描述`
- `面试高压追问`
- `投递行动计划`
- `复盘表`

窄输出规则：

- 用户只要“项目推荐”：只执行 Phase 1。
- 用户只要“二次开发”：要求先确认项目，然后执行 Phase 2。
- 用户只要“简历”：如果没有项目成果，先提示风险；如果没有明确项目，先执行最小 Phase 1 项目确认；如果已有项目但没完成改造，可生成草稿但必须标注待验证。
- 用户只要“面试”：如果没有项目成果，先要求用户提供项目摘要或简历 bullet；如果没有项目，先执行最小 Phase 1 项目确认；再读取 `job_execution_playbook.md` 追问。

最小 Phase 1 定义：

- 用于窄输出场景，不等同于完整 Phase 1。
- 必须完成：确认目标岗位、确认用户水平或做低风险假设、获得一个明确项目对象。
- 如果用户没有项目：搜索或让用户提供 1 到 3 个候选仓库，并使用 `github_scoring_matrix.md` 做轻量评分。
- 轻量评分只保留项目名称/链接、岗位匹配、学习价值与阶段匹配、主要风险、是否建议继续。
- 不输出完整得分雷达、完整岗位拆解或多项目深度对比。
- 保留卡点：必须让用户确认一个项目，或由用户明确说“直接用第一个/最高分项目继续”。
- 简历窄输出的最小 Phase 1 额外要求：至少获得项目背景、用户完成内容或计划完成内容。
- 面试窄输出的最小 Phase 1 额外要求：至少获得项目摘要、核心技术栈和 1 到 3 个可追问的技术点。

## 阶段匹配扣分锚点

`github_scoring_matrix.md` 必须给出“学习价值与阶段匹配”的扣分锚点，避免模型凭感觉判断难度。

示例规则：

- 用户只会基础语法，项目包含微服务、Kafka/RocketMQ、分布式事务、Kubernetes 等多组件链路：阶段匹配维度至少扣 15 分，并触发降级学习路线。
- 用户只会框架 CRUD，项目要求理解框架源码、编译器、数据库内核或复杂分布式一致性：阶段匹配维度至少扣 10 分。
- 用户时间预算少于 2 周，项目无法在 1 到 2 天内跑通最小闭环：阶段匹配维度至少扣 10 分。
- 项目技术栈豪华但业务闭环不清、无法形成可讲项目：学习价值不得高于该维度一半。
- 项目较难但可拆成最小启动、模块阅读、单点二开和可验收成果：可以保留推荐，但必须输出降级学习路线。

## 测试策略

实现必须使用 TDD：

1. 先更新测试，验证 v2 结构缺失时失败。
2. 创建五个 reference 文件的最小内容，让文件存在性测试通过。
3. 更新 `SKILL.md` 为主控引擎，让调度规则和三阶段工作流测试通过。
4. 补足 reference 内容，让关键知识点测试通过。
5. 运行 skill 校验，并同步到 Codex skill 库。

测试必须验证：

- `zero-to-job-project-coach/references/` 存在。
- 五个 reference 文件存在。
- `SKILL.md` 引用五个 reference 文件。
- `SKILL.md` 包含 `Reference 调度规则` 和 `三阶段解锁工作流`。
- `SKILL.md` 的 `关键规则` 包含不承诺 offer、不只按 star 推荐、不编造 GitHub 精确统计、可信简历、卡点风险和按需读取 reference。
- `SKILL.md` 包含 `输入与冲突处理`，并覆盖低风险默认、高风险追问、水平/JD 冲突。
- `SKILL.md` 包含 `状态管理与阶段声明`，并要求每次回复以 `[当前阶段：...]` 开头。
- `SKILL.md` 包含 `Reference 输出边界`，禁止 Phase 1 输出 Phase 3 高压面试题，禁止 Phase 2 提前生成正式简历 bullet。
- `SKILL.md` 包含 Phase 1、Phase 2、Phase 3。
- `SKILL.md` 明确阶段卡点和允许预览 Phase 3 的风险提示。
- `SKILL.md` 包含验收失败纠偏机制，禁止用户未通过验收时无风险进入 Phase 3。
- `SKILL.md` 包含私有/企业项目处理规则，并要求保护敏感信息。
- `SKILL.md` 包含全局 `降级路径`，并区分无法联网、信息不足、无法核验源码、跳过实操和 reference 缺失。
- `agents/openai.yaml` 的 `display_name`、`short_description` 和 `default_prompt` 符合三阶段求职推进定位，并引用 `$zero-to-job-project-coach`。
- `role_templates.md` 覆盖 Java 后端、前端、AI 应用开发、数据分析、DevOps。
- `role_templates.md` 还必须覆盖全栈和移动端开发，保证不低于 v1 的七类岗位覆盖。
- `role_templates.md` 将 TextSplitter 放在 AI 应用开发进阶验收点，而不是入门底层基线。
- `github_scoring_matrix.md` 包含 100 分评分矩阵、学习价值与阶段匹配维度和一票否决规则。
- `github_scoring_matrix.md` 包含阶段匹配扣分锚点和降级学习路线触发条件。
- `secondary_dev_blueprints.md` 包含 Level 1、Level 2、Level 3 改造蓝图。
- `resume_benchmarks.md` 包含 STAR、低效表达、高阶表达和量化指标规则。
- `job_execution_playbook.md` 包含高压追问、投递执行表和 1 到 2 周投递节奏。
- `job_execution_playbook.md` 要求投递执行表用 Markdown 表格或 CSV 代码块输出。
- v1 中关于不编造 GitHub 精确统计、可信简历、技术选型与踩坑、防卡壳路线、源码结构评估、窄输出的核心约束不能回退。

### 测试迁移策略

v1 测试中原本检查单个 `SKILL.md` 的断言要迁移到合适位置：

- 仍检查 `SKILL.md`：frontmatter、主控章节、reference 调度、三阶段工作流、阶段卡点、全局降级路径、输出控制、openai.yaml 元数据。
- 迁移到 `role_templates.md`：岗位覆盖、岗位技术底线、里程碑验收。
- 迁移到 `github_scoring_matrix.md`：搜索/评分策略、不可编造 issue/PR 精确统计、学习价值与阶段匹配、一票否决。
- 迁移到 `secondary_dev_blueprints.md`：二次开发层级、技术锚点、可交付物、验收问题、预期指标。
- 迁移到 `resume_benchmarks.md`：可信简历、STAR、量化指标、低效/高阶表达对比。
- 迁移到 `job_execution_playbook.md`：技术选型与踩坑、高压追问、投递节奏、复盘表。

### 行为评测边界

除文件存在和关键词测试外，v2 需要增加行为评测用例。第一版可以用 Markdown 或 JSON 保存 eval case，不要求自动调用 LLM，但必须作为后续人工或自动评测基线。

建议新增 `tests/behavior_eval_cases.md`，至少包含：

- 用户要求“跳过 Phase 2，直接写简历”：期望输出包含未完成实操带来的简历风险。
- 用户在 Phase 1 后未确认项目却要求二次开发：期望输出要求先确认项目或执行最小 Phase 1。
- 用户提供私有实习项目描述：期望输出跳过 GitHub scoring，要求业务架构摘要和核心难点。
- 用户回答验收问题错误：期望输出指出错误、给 Hint、要求重答，不直接进入 Phase 3。
- 用户只要投递计划：期望输出 Markdown 表格或 CSV 代码块。

测试脚本至少要验证该行为评测文件存在，并包含上述场景标题和期望风险语句。

## 验收标准

v2 完成条件：

- 所有测试通过。
- `quick_validate.py` 校验通过。
- `SKILL.md` 主体比当前版本更像调度器，而不是知识百科。
- 五个 reference 文件内容可独立维护。
- 行为评测用例文件存在，并覆盖跳过阶段、私有项目、验收失败和投递表格式。
- Codex skill 库中的 `zero-to-job-project-coach` 已同步更新。
- 不提交 `docs/superpowers/`。
- 不提交 `.idea/`。

## 风险与缓解

风险：reference 文件过大，导致维护困难。  
缓解：第一版只写高频岗位和高价值案例，后续增量扩展。

风险：阶段卡点让用户觉得流程太慢。  
缓解：允许用户显式要求预览后续阶段，但必须提示未实操的简历风险。

风险：评分矩阵看似精确但依据不足。  
缓解：分数必须基于可见证据；没有证据时降低置信度，不编造 issue/PR 精确统计。

风险：简历 bullet 过度包装。  
缓解：强制区分已完成、预期目标和待实测指标。

## 自检

本规格没有占位项。范围聚焦在一个 skill 的 v2 架构升级，文件边界清晰，TDD 验收项覆盖主控引擎、五个 reference、阶段卡点、降级路径和同步要求。
